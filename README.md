# 会话断点 (checkpoint)

当前版本：v1.1.0

Claude Code 会话结束时自动生成断点笔记，写入 Obsidian 知识库——主题命名、大小类标签、状态 triage、可恢复。断点笔记 + 每日索引 + 首页仪表盘 + Bases 视图，让你随时找到“上次那件事聊到哪了”。

Stop Hook 会在检测到本次会话写入 `Claude方案/<项目名>/` 后，自动刷新项目级滚动总结，并把跨项目可迁移经验沉淀到同类设计主题文件。下一次接手同项目时先读 `项目总结.md`，同类项目先读 `AI开发参考/`，避免为了恢复上下文反复加载完整长 transcript。

> `v1.0.0` 是裁剪后的首个公开版本：仅保留 Claude Code + Obsidian 知识库运行功能，仓库不再包含安装、卸载、迁移、打包或解包脚本。

## 版本说明

### v1.1.0

检索增强版本，为断点、项目总结和 AI 开发参考增加统一的别名与关键词元数据。

- 新生成或自然刷新的文档统一写入 `aliases` 和 `keywords`，保留已有手工元数据并只补齐缺失字段。
- 修复旧断点非 `--force` 刷新时的 frontmatter 四字段解包错误。
- LLM 不可用时从层级 tags 的末级词保守补充 keywords，并过滤用户目录、临时目录和 worktree 等路径噪声。
- `/search` 按 aliases、keywords、tags、正文的顺序排序和去重，仅读取最相关的 1-2 篇。
- 新增元数据测试与临时 vault 端到端验证，覆盖连续刷新、项目总结、AI 开发参考及全文搜索兜底。

### v1.0.3

安全补丁版本，为断点笔记、项目总结和 AI 开发参考增加敏感信息脱敏保护。

- 自动识别并替换 Bearer Token、API Key、访问令牌、密码、JWT、`sk-` 密钥及 `X-Namespace` 等敏感内容。
- 在读取会话记录、生成断点、刷新每日索引、项目总结、AI 开发参考和知识库首页时统一执行脱敏。
- 对本次会话写入 `Claude方案/` 的 Markdown 文件执行脱敏检查。
- 新增脱敏测试，覆盖常见凭据格式、生成内容和文件写入流程。

### v1.0.2

补丁版本，统一同类经验目录命名为 AI开发参考，并规范每日索引 tags 格式。

- Stop Hook 生成和刷新跨项目经验时，统一写入 `Claude方案/AI开发参考/<同类设计主题>.md`。
- `CLAUDE.md` 和 README 中的恢复规则同步为优先读取 `AI开发参考/`。
- 新生成的每日会话索引 frontmatter 使用 `tags: [claude/会话索引]` 内联数组格式。

### v1.0.1

补丁版本，完善会话断点按年月日归档与递归检索。

- 会话断点写入路径调整为 `Claude方案/会话断点/YYYY-MM/DD/`，便于按月份和日期浏览。
- 历史断点迁移后，索引、首页、项目总结和可复用经验使用完整路径 wikilink，避免跨目录解析错误。
- `/search` 与 `/synthesize` 改为递归检索断点目录，覆盖日期子目录。
- 修正历史断点按每日索引日期归档，避免因旧 frontmatter 日期不准导致同月记录集中到单日。

### v1.0.0

首个公开版本，聚焦 Claude Code + Obsidian 知识库的会话断点、项目总结和经验复用闭环。

- 自动生成会话断点：会话结束后写入 `Claude方案/会话断点/YYYY-MM/DD/`，记录状态、恢复命令和本次产出。
- 主题化断点命名：文件名和 H1 优先使用会话主题，避免 URL、session id、原始长问题进入断点标题。
- 同步每日索引和首页仪表盘：按日期索引断点，并汇总标签、状态和待恢复会话。
- 支持项目滚动总结：写入 `Claude方案/<项目名>/` 后自动刷新 `项目总结.md`，作为下次接手的首读摘要。
- 沉淀同类 AI 开发参考：经验按 `AI开发参考/<同类设计主题>.md` 聚合，不再按项目拆散。
- 递归检索与扫描知识库：覆盖 `网站平台汇总/<项目名>/` 等二级目录资料。
- 保留轻量运行边界：仓库只包含 Claude Code + Obsidian 知识库运行功能与规则文件，不包含安装、卸载、迁移、打包、解包脚本。

## 日常怎么用

1. **正常聊天**——会话结束自动生成断点。
2. **想回顾**——打开 `_知识库首页.md` 看总览，或翻 `Claude方案/会话索引/` 每日索引。
3. **没干完的事**——笔记里有 `claude --resume <id>`，拿 id 恢复接着干。
4. **方案敲定了**——让 Claude 把方案 Write 到 `Claude方案/<项目>/`，断点自动变 ✅。
5. **下次接手同项目**——新会话先读 `Claude方案/<项目>/项目总结.md`，再补读 1-2 篇关键方案/修复记录。
6. **做同类项目**——先读 `Claude方案/AI开发参考/<同类设计主题>.md`，复用参考摘要里的流程、坑点和验收清单。
7. **知识积累多了**——跑 `/synthesize` 把同类断点合成知识文档。

## 功能

- **自动断点**：Claude Code Stop Hook 在会话结束时按日期生成断点笔记。
- **主题化命名**：会话断点文件名和 H1 使用主题名，不使用 session id、URL 或原始长问题。
- **大小类标签与检索元数据**：LLM 一次调用产出主题、`category`、`tags`、`keywords`，并组合稳定的 `aliases`。
- **状态 triage**：✅ 完成 · ⚠️ 中断 · 📋 方案未归档。纯问答不误标 ⚠️。
- **知识库首页**：`_知识库首页.md` 自动刷新，概览标签云、待恢复列表和完成率。
- **PreToolUse 提醒**：往 `Claude方案/` 写文件时自动提醒已有相关文档。
- **项目滚动总结**：写入 `Claude方案/<项目名>/` 后，自动刷新 `项目总结.md`。
- **同类 AI 开发参考**：经验沉淀到 `Claude方案/AI开发参考/<同类设计主题>.md`，不再按项目拆成 `<项目名>-AI开发参考.md`。
- **递归检索与扫描**：覆盖 `网站平台汇总/<项目名>/` 这类二级目录资料。
- **知识合成 + 搜索**：`/synthesize` 按标签合并同类断点，`/search` 优先按 aliases、keywords、tags 排序检索，并用正文递归搜索兜底。

## 命令

| 命令 | 作用 |
|---|---|
| `/checkpoint` | 手动生成或刷新当前会话断点 |
| `/search <关键词>` | 元数据优先搜索知识库：别名、关键词、标签及正文 |
| `/synthesize` | 按标签或主题合并同类断点，生成知识文档 |

## 目录结构

```text
vault/
├── _知识库首页.md
└── Claude方案/
    ├── 会话索引/                    # 每日索引 YYYY-MM-DD.md
    ├── 会话断点/YYYY-MM/DD/           # 断点笔记 <主题>.md
    ├── AI开发参考/                  # 同类设计主题经验
    ├── 网站平台汇总/<项目名>/        # 平台类项目集中归档
    ├── <项目名>/                    # 归档方案 + 项目总结.md
    └── 会话断点.base                # Bases 视图
```

```text
.claude/
├── hooks/
│   ├── checkpoint.py                # Stop Hook 主逻辑
│   └── pretool.py                   # 写入 Claude方案 前提醒相关文档
├── skills/
│   ├── checkpoint/
│   ├── checkpoint-lite/
│   ├── search/
│   └── synthesize/
└── settings.json
```

## 当前规则要点

1. 技术、方案、配置、运维、开发类问题回答前先递归检索 `Claude方案/`。
2. 平台类资料集中在 `Claude方案/网站平台汇总/<项目名>/`。
3. 同类经验集中在 `Claude方案/AI开发参考/<同类设计主题>.md`。
4. 会话断点标题按主题命名，session id 只放在 frontmatter 和恢复命令里。
5. Hook 扫描项目材料和仪表盘统计时使用递归扫描，覆盖二级目录。
6. 仓库只保留项目运行功能与规则文件，不保留安装、卸载、迁移、打包、解包脚本。
